import React from 'react'
import { ReactComponent as Logo } from '../assets/logo.svg'

import { Flex } from '../react-atomic'

const Header = () => {
    return (<Flex >
        <Flex>
            <Logo />
            wadus
        </Flex>
    </Flex>)
}

export default Header;