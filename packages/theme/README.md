# @ract-atomic-ui/theme

Global theme object for react-atomic-ui system

## theme

```jsx
{
    base: {},
    components: {},
}
```

- `base` would contain common configs
- `components` would define each component config
